// src/utils/greetings.ts

export function generateGreeting(name: string): string {
  return `Hello, ${name}! Welcome!`;
}

export const farewell = (name: string): string => {
  return `See you later, ${name}. Come back soon!`;
};
